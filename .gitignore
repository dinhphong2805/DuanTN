# Maven / Java build
Backend/target/

# Eclipse (mở project trực tiếp trong IDE) — không cần trên Git
Backend/.classpath
Backend/.project
Backend/.settings/

# Cấu hình local (API key Gemini, v.v.) — KHÔNG đẩy lên Git dù repo private
# Dùng application-local.properties.example làm mẫu; mỗi máy tự tạo file local.
Backend/src/main/resources/application-local.properties

# Ảnh upload local
Backend/uploads/

# Frontend
Frontend/node_modules/
Frontend/dist/

# IDE — repo private: có thể commit .vscode/ nếu muốn đồng bộ editor
.idea/
*.iml
